doctype html
html
  head
    title Test
    meta(charset='utf-8')
    meta(http-equiv='Content-Type', content='text/html; charset=UTF-8')
    script(src='system.js')
    script(src='jspm.config.js')
    script(src='bundle.js')
    script(type='text/javascript').
      System.import('test/browser/bundleme')
        .then(function() {
          document.addEventListener('DOMContentLoaded', function() {
            document.body.innerHTML += '<h2>Loading done</h2>';
          });
        });
  body
    h1 I should become blue and background should show a SASS logo on a white background
    h2 Loading...
    p I SHOULD BE ALL LOWERCASE UNDERLINED AND CENTERED
    .alert.alert-success I should be a bootstrap success alert
    .flexContainer
      .fbox Flex Box 1
      .fbox Flex Box 2 should be right to 1
      .fbox Flex Box 3 should be right to 2
    p.spaced I am from a directory with a space in path and should become yellow
